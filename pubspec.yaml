name: macro_standard_installation_test
description: A sample command-line application.
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ">=3.4.4 <4.0.0"

# Add regular dependencies here.
dependencies:
  # path: ^1.8.0

dev_dependencies:
  custom_lint: ^0.6.4
  macro_union_types:
    path: ./packages/macro_union_types
  #lints: ^4.0.0
  #test: ^1.24.0
